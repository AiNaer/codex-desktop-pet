# Codex 桌宠

根据我的好友vn形象做出的桌宠。

这是一个 Windows 像素风桌宠：保留照片形象的大头、火柴人身体和白色描边，启动后会轻微摇头；不会自动在桌面上移动，但可以手动拖动。

## 运行

1. 安装 Python 3 和 Pillow：

   ```powershell
   python -m pip install -r requirements.txt
   ```

2. 双击 `启动Codex桌宠.bat`。

## 操作

- 左键拖动桌宠
- 右键退出
- 按 `Esc` 退出
